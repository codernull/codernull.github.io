# 低延迟系统

行情推送、抖动、唤醒路径——用测量和链路证据说话，不做「优化清单」式空谈。

## 文章

| 文章 | 一句话结论 |
|---|---|
| [撮合引擎核心：订单簿结构、撮合语义与并发读取](./撮合引擎核心) | 正确性九成在数据结构选型与撮合语义边界：Skiplist + 每档 FIFO + 订单索引实现价格/时间优先，成交价归被动方 |
| [Redis 还是单线程：6.0/8.x 改了外围，热路径不该指望它](./redis-还是单线程) | 命令执行始终在主线程串行，IO 线程只搬读写不搬执行；热路径延迟瓶颈在主线程时，升版本救不了 |
| [Redis 缓存设计：热 key、区间扫描、修正失效与集群批量](./redis-缓存设计) | 先认访问模式再选原语：覆盖写 STRING+TTL 心跳、区间 ZSET+HASH、整页 STRING+代次换键、批量作废 INCR；Lua 原子 + Cluster hash tag，集群批量按节点分组 |

## 计划中

- Docker / 宿主机路径下的延迟与抖动结论
- 信号唤醒 vs 轮询：何时值得改
- Aeron 在行情链路里能替代什么
