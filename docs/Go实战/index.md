# Go 实战

从真实工程约束出发的 Go 笔记：协议、运行环境、编辑器链路。

## 文章

| 文章 | 一句话结论 |
|---|---|
| [WSL + Neovim 配置全记录（可复现，不是插件安利）](./wsl-neovim-配置全记录) | Snap 版 Neovim + 纯 Lua 覆盖比 apt 版 + `.vimrc` 混合更可维护，迁移路径保留 `.vimrc.backup` 可回滚 |

## 计划中

- WebSocket 客户端从零到一（重连、背压、心跳）
